import kotlinx.datetime.Instant;

CREATE TABLE `users`(
    id INTEGER NOT NULL PRIMARY KEY,
    login TEXT NOT NULL,
    passwordHash TEXT NOT NULL,
    displayName TEXT,
    apiToken TEXT,
    apiTokenExpiration INTEGER AS Instant

);